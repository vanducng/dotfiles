[Unit]
Description=User-space OpenSSH on port 2222 (no sudo)
After=network-online.target homelab-disks.service
Wants=network-online.target

[Service]
Type=simple
ExecStart=%h/.local/opt/openssh/usr/sbin/sshd -D -f %h/.config/sshd/sshd_config -h %h/.config/sshd/ssh_host_ed25519_key -o PidFile=%h/.config/sshd/sshd.pid
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
