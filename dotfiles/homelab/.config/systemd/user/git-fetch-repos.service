[Unit]
Description=Fetch curated work clones (no merge)
After=network-online.target

[Service]
Type=oneshot
ExecStart=%h/.local/bin/git-fetch-repos
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
